name = "docbuild"
reservoir = false
version = "0.1.0"
packagesDir = "../.lake/packages"

[[require]]
name = "InfinitaryLogic"
path = "../"

# Pinned to an exact commit on doc-gen4's main branch, not to a moving branch name.
# There is no v4.34.0 tag; main is on leanprover/lean4:v4.34.0-rc1, matching the root
# toolchain. Bump this together with the root toolchain.
[[require]]
scope = "leanprover"
name = "doc-gen4"
rev = "db53577d4634df2604840cabb4bc74685300afe4"
